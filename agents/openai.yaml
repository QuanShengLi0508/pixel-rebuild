interface:
  display_name: "Python 1:1 Pixel Rebuild"
  short_description: "Measure, redraw, compare, and verify raster figures in Python"
  default_prompt: "Use $pixel-rebuild to recreate this reference figure 1:1 as standalone Python drawing code and verify the rendered output."
